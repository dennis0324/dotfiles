[project]
name = "install-helper-dev"
version = "0.1.0"
description = "Ansible playbook 설치 항목 선택 헬퍼 (questionary)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "questionary>=2.0.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "nuitka>=2.0",
]

